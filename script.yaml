notify:
  sequence:
    - service: notify.slack
      data_template:
        message: "{{ message }}"
    - service: notify.tts_sonos
      data_template:
        message: "{{ message }}"
    - service: notify.atv_shield
      data_template:
        message: "{{ message }}"
    - service: notify.atv_nexusplayer
      data_template:
        message: "{{ message }}"


